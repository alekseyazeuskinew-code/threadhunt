# Threadhunt — полный цикл кандидата (CRM), дизайн

Оцифровка реального процесса найма в канбан, где у каждой стадии — свои фичи.

## Твой текущий процесс (как есть)
1. Бот шлёт авто-отбивку в директе Threads.
2. Кандидат пишет ассистенту в Telegram.
3. Ассистент отправляет условия + тестовое задание.
4. Кандидат сдаёт тестовое за 1–2 часа.
5. Нравится тест → начинаем работу.

## Оцифровка: стадии канбана + фичи на каждой

| Стадия | Что значит | Фичи на карточке |
|---|---|---|
| **Новый** | поймали по кодовому слову (авто) | источник (Запросы/Скрытые), слово, время; кнопка «взять в работу» |
| **На связи** | перешёл к ассистенту в TG | поле TG-контакта; кнопка «отправил условия» (ставит timestamp); шаблон условий в 1 клик |
| **Тест / собес** | выдали тестовое | «тест выдан» + **дедлайн-таймер 1–2 ч** (обратный отсчёт); поле «ссылка на сдачу»; «сдано в …»; быстрая оценка ⭐ + ✓/✗ |
| **В команде** | взяли | дата старта, роль, ставка; заметка-онбординг |
| **Резерв** | держим тёплым | дата следующего касания; авто-напоминание «разогреть» |
| **Отказ** | не подошёл | причина (тест/темп/цена/скилл) — копится в аналитику |

## Фичи цикла (что вшить)
- **Чек-лист по стадии** — у каждой стадии свои пункты (условия отправлены → тест выдан → тест сдан → решение). Прогресс виден на карточке.
- **Таймеры/SLA** — «ответил ли за N часов», «сдал ли тест за 1–2 ч». Просрочка подсвечивается — видно, кто остыл.
- **Шаблоны сообщений** на стадию (условия, тестовое, оффер, вежливый отказ) — копируешь и шлёшь в TG; ИИ их генерирует под роль.
- **Воронка под роль** — у каждого вида сотрудника свой набор стадий/чек-листов/тестового (у монтажёра один тест, у таргетолога другой). Шаблон воронки выбирается при создании поиска.
- **Авто-переходы** — пришёл лид → «Новый»; ответил бот → отметка; (позже, когда подключим события) ответ кандидата → «На связи».
- **Таймлайн кандидата** — лента: пойман → ответили → условия → тест → решение, с временами. Уже есть комментарии — расширяем до событий.
- **Аналитика воронки** — конверсия по стадиям, среднее время до найма, % сдавших тест, причины отказов.

## Реализация (предложение по этапам)
1. **Данные**: расширить `Lead` полями цикла — `contact` (TG), `conditionsSentAt`, `testSentAt`, `testDeadlineAt`, `testSubmittedUrl`, `testSubmittedAt`, `decisionReason`, `startedAt`, `role`, `rate`, `nextTouchAt`. + событийный таймлайн (`LeadEvent`).
2. **Карточка кандидата (drawer)**: на каждую стадию — свой блок действий (кнопки/поля выше), таймер дедлайна, чек-лист.
3. **Шаблоны сообщений на стадию** + ИИ-генерация (переиспользуем движок генерации).
4. **Воронки под роль**: пресет роли (`roleTemplates`) расширяем до funnel-пресета (стадии + тестовое + шаблоны).
5. **Аналитика воронки** на странице поиска и в Обзоре.

Минимализм сохраняем: на карточке показываем ТОЛЬКО блок текущей стадии, остальное — в таймлайне.
