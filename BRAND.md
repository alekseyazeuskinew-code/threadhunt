# Threadhunt — бренд-гайд

## Имя

**Threadhunt** = **Thread**(s) + head**hunt**.

Миксовое название читается мгновенно в обе стороны:
- **Thread** — площадка Threads, «нить» разговора, лента, в которой мы постим приманки.
- **hunt** — хедхантинг, активный поиск и наём.

Позиционирование: **«VIP-хедхантинг через Threads на автопилоте»**. Ты ставишь приманки, Threadhunt ловит и квалифицирует лидов сам.

> Торговая марка: слово «thread» общеупотребимо, бренд про найм — отдельный класс от соцсети Meta. Если позже понадобится дистанция — безболезненный фолбэк `Loomhunt` / `Wefthunt` (меняется только брендинг, не код).

### Произношение и написание
- Пишется слитно, одно слово: **Threadhunt** (не «Thread Hunt», не «ThreadHunt» в текстах — только лого может играть регистром).
- Домены-цели: `threadhunt.app` / `.io` / `.co`.

### Tagline (варианты)
- RU: «Лента Threads → поток кандидатов».
- RU: «Ставь приманку. Threadhunt ловит сам».
- EN: «Turn Threads into a talent funnel».

## Tone of voice

Уверенный, дружелюбный, по делу. Минимум маркетингового шума, максимум конкретики. Обращение на «ты». Глаголы действия: создай, запусти, поймай, направь. Эмодзи — точечно, не как декор.

## Палитра — «Lime-заряд»

Тёмный холст, один яркий акцент. Отстраивает от чёрно-белого Threads, читается как современный дев-тул.

| Токен | HEX | Назначение |
|---|---|---|
| `bg` | `#0B0B0F` | основной фон (почти-чёрный) |
| `panel` | `#14141A` | карточки, панели |
| `panel-2` | `#1C1C24` | вложенные блоки, hover |
| `border` | `#26262F` | границы |
| `text` | `#EDEDF2` | основной текст |
| `muted` | `#8A8A99` | вторичный текст |
| `accent` | `#C6F24E` | лайм — кнопки, активные состояния, акценты |
| `accent-press` | `#B2DE3C` | нажатие/hover акцента |
| `accent-soft` | `rgba(198,242,78,.12)` | фон-подложка под акцент |
| `success` | `#4ADE80` | успех |
| `danger` | `#FB7185` | ошибки, удаление |
| `warning` | `#FBBF24` | предупреждения |

Правило акцента: лайм — **только** для главного действия экрана и активных состояний. Не больше одного «зелёного» пятна в поле зрения. Текст на лайме — всегда тёмный (`#0B0B0F`), не белый.

## Шрифты

| Роль | Шрифт | Где |
|---|---|---|
| Заголовки / лого | **Space Grotesk** | h1–h3, вордмарк, крупные числа |
| Текст / интерфейс | **Inter** | абзацы, кнопки, формы, таблицы |
| Моно / данные | **JetBrains Mono** | id, токены, метрики, код |

Все три — Google Fonts (бесплатные), подключаются через `next/font`. Заголовки — плотный трекинг (`tracking-tight`), вес 600–700.

## Логотип / знак

Концепт: **игла с нитью, образующая «галочку-прицел»** — нить (Threads) проходит сквозь ушко и складывается в «hunt»-отметку (поймано).

Текстовый вордмарк (для шапки/MVP, пока нет графики):

```
  ⟋⟋  threadhunt
```

- Строчными, Space Grotesk 600, `tracking-tight`.
- Перед словом — две лаймовые наклонные «нити» `⟋⟋` (или SVG-штрихи) как опознавательный знак.
- Иконка-фавикон: квадрат `bg`, в центре две лаймовые нити под углом + точка-«ушко».

## Иконки

Outline-стиль, тонкая обводка (lucide-react). Никаких заливок, кроме активного состояния (тогда — лайм).

## Применение в UI (резюме)

- Тёмный фон везде, одна акцентная кнопка на экран.
- Скругления: `rounded-2xl` карточки, `rounded-full` главные кнопки и чипы.
- Воздух: щедрые отступы, крупная типографика заголовков.
- Состояния (активен/пауза, онлайн/оффлайн) — чипы с мягким цветным фоном, не «светофор».
